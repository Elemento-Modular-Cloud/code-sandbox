#cloud-config

users:
  - default
  - name: elemento-root
    password: "0000"
    shell: /bin/bash
    lock_passwd: false
    ssh_pwauth: True
    chpasswd: { expire: False }
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: users, admin